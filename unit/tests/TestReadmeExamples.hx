package tests;

import macrotween.Tween;
import tests.HelperObjects;
import utest.Assert;
import utest.ITest;

/**
 * Introductory example code for the MacroTween tweening library readme
 */
class TestReadmeExamples implements ITest {
	public function new() {}

	// TODO
	/*
	public function testSimpleTween() {
		//var myObject:{ x:Float } = { x: 0 };
		
		// Tween myObject.x to 100
		//var tween = Tween.tween(0, 1, myObject.x => 100, Ease.quadInOut);
		
		//tween.stepTo(0, null, true);
		//Assert.isTrue(myObject.x == 0);
		
		//tween.stepTo(1, null, true);
		//Assert.isTrue(myObject.x == 100);
	}
	
	public function testTweenWithRanges() {
		//var myObject:{ x:Float } = { x: 0 };
		
		// Specify ranges
		//var tween = Tween.tween(0, 1, myObject.x => 10...20);
	}
	
	public function testTweenWithLocalVariable() {
		// Tween local variables
		//var x:Float = 10;
		//Tween.tween(0, 1, x => 100);
	}
	
	public function testTweenMultipleObjects() {
		// Tween multiple objects at once
		//Tween.tween(0, 1, [object1.x => 100, object2.x => 200]);
	}
	
	public function testCompoundExpressions() {
		//Tween.tween(0, 1, obj => [x => 100, y => 100]);
		//Tween.tween(0, 1, [objA, objB] => [x => 100, y => 100]);
		//Tween.tween(0, 1, mySprite.scale => [[x, y] => 100]]);
	}
	
	public function testImplicitStartOrEndValues() {
		// Implicit start or end values
		// A reverse tween from t=1...0 would tween from whatever the initial value is to 10
		//Tween.tween(0, 1, myObject.x => 10..._);
	}
	
	public function testFunctionTween() {
		// Repeatedly call function with tweening parameters
		//Tween.tween(0, 1, myFunc(10...20));
	}
	*/
}